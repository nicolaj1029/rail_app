# Policy Context (EU 2021/782) — Short Operational Summary

This file is read by the AI audit command as part of the regulatory context. Keep it concise and actionable.

- Article 12: Assistance, rerouting and reimbursement rules (TRIN2/7 incl.).
- Article 17–20: Compensation percentages and thresholds.
- National deviations/overrides are defined in `config/data/national_overrides.json`.
- Exemptions per market/scope captured in `config/data/exemption_matrix.json`.
- Any additional operator-specific notes captured in `docs/rules_snapshot.txt` (or `config/data/rules_snapshot.txt`).

Tips:
- Prefer bullet points and examples over long prose.
- Include concrete thresholds (e.g., FR G30 30/60/120 min, DK Rejsetidsgaranti bins, SE regional <150 km gates).
